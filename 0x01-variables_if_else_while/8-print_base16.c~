#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int letter;

	for (letter = 0x00H ; letter <= 0x0FH ; letter++)
	{
		putchar(letter);
	}
	letter = '\n';
	putchar(letter);
	return (0);
}
