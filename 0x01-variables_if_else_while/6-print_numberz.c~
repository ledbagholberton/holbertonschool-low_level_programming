#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int letter;

	for (letter = 65 ; letter <= 89 ; letter++)
	{
	putchar(letter);
	}
	letter = '\n';
	putchar(letter);
	return (0);
}

