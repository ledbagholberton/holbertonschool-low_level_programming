#include <stdio.h>


/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int minumero;

	for (minumero = 0 ; minumero <= 9 ; minumero++)
	{
		printf("%d\n", minumero);
	}
	return (0);
}
